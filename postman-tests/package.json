{
    "scripts": {
        "test": "npx newman run -e workspace.postman_globals.json \"Swagger Petstore - OpenAPI 3.0.postman_collection.json\" "
    },
    "devDependencies": {
        "newman": "^5.3.2"
    }
}
